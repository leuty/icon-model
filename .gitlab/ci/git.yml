# ICON
#
# ---------------------------------------------------------------
# Copyright (C) 2004-2025, DWD, MPI-M, DKRZ, KIT, ETH, MeteoSwiss
# Contact information: icon-model.org
# See AUTHORS.TXT for a list of authors
# See LICENSES/ for license information
# SPDX-License-Identifier: BSD-3-Clause
# ---------------------------------------------------------------

.git_sparse:
  variables:
    GIT_STRATEGY: empty
    SOURCE_DIR: ${CI_PROJECT_DIR}
    SPARSE_PATTERNS:
    SUBMODULES:
  before_script:
    - module purge
    - module load git
    - git init --quiet "${SOURCE_DIR}"
    - git -C "${SOURCE_DIR}" sparse-checkout set --no-cone -- ${SPARSE_PATTERNS}
    - git -C "${SOURCE_DIR}" remote add origin "${CI_REPOSITORY_URL}"
    - git -C "${SOURCE_DIR}" fetch --filter=blob:none --depth=1 --jobs=4 origin "${CI_COMMIT_SHA}"
    - git -C "${SOURCE_DIR}" checkout --quiet FETCH_HEAD
    - git -C "${SOURCE_DIR}" submodule update --init --depth=1 --jobs=4 -- ${SUBMODULES}

.git_jacamar:
  variables:
    GIT_DEPTH: 3
    GIT_SUBMODULE_STRATEGY: normal
    GIT_SUBMODULE_DEPTH: 1
    GIT_SUBMODULE_PATHS:
    GIT_SUBMODULE_UPDATE_FLAGS: --jobs=4
