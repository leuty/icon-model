#!/bin/bash

# ICON
#
# ------------------------------------------
# Copyright (C) 2004-2026, DWD, MPI-M, DKRZ, KIT, ETH, MeteoSwiss
# Contact information: icon-model.org
# See AUTHORS.TXT for a list of authors
# See LICENSES/ for license information
# SPDX-License-Identifier: BSD-3-Clause
# ------------------------------------------

set -eu
unset CDPATH
ulimit -c 0

SCRIPT_DIR=$(cd "$(dirname "$0")"; pwd)

export HK_GPU_MPIRUN_FLAGS=''
export HK_GPU_FCFLAGS='-acc=verystrict -gpu=cc80'
${SCRIPT_DIR}/hk.common.nvhpc-24.7 --enable-gpu --disable-mpi-gpu "$@" # 2025-05-27: dwd_run_ICON_09_R2B4N5_EPS encounters UCX error with mpi-gpu enabled
