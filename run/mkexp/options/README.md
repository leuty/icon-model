# mkexp for ICON

## `link_output_files`

Create an inline `link` job which is added to each `run` job
and links all output files in the work directory to the data directory.
This is mutually exclusive to the standard post-processing job (`post`).
