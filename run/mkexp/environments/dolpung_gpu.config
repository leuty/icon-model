# ICON
#
# ------------------------------------------
# Copyright (C) 2004-2025, DWD, MPI-M, DKRZ, KIT, ETH, MeteoSwiss
# Contact information: icon-model.org
# See AUTHORS.TXT for a list of authors
# See LICENSES/ for license information
# SPDX-License-Identifier: BSD-3-Clause
# ------------------------------------------

# This environment allows to run experiments at
# the GPUs of the "dolpung" partition of Levante (DKRZ).
# It distributes the IO tasks among the CPUs of the nodes.

VERSION_ = $$Id: run/mkexp/environments/dolpung_gpu.config 0 2026-01-14 13:34:00Z k202123 $$

ACCOUNT = """eval("$account_" if "$account_" else feedback.die("please set ACCOUNT to your levante compute project, either in your experiment config or on the command line, or save the project name to '$HOME/.acct'"))"""

INPUT_ROOT = /pool/data
WORK_ROOT = "eval('/scratch/'+'$USER'[0]+'/$USER')"
DATA_ROOT = /work/$PROJECT_SUBDIR/$USER

# Distribution of the IO tasks among the CPUs of the GPU nodes
#  it calculate num_io_procs based on choosen number of nodes and num_io_tasks
IO_DISTRIBUTION_OVER_GPUS = true
[namelists]
  [[NAMELIST_atm]]
    [[[parallel_nml]]]
      num_io_procs = raw($${num_io_procs})

[jobs]
  ldd = ldd
  batch_command = sbatch
  progress_command = sh -c 'scontrol update JobId=$$SLURM_JOB_ID Comment="$$*"' scomment
  ksh = /bin/bash

  # Environment variables for job information
  name_environ = SLURM_JOB_NAME
  id_environ = SLURM_JOBID

  # Hardware parameters
  default_gpus_per_node = 4    # = 4 sockets/node x 1 GPUs/socket
  default_cpus_per_node = 288  # = 4 sockets/node x 72 CPU cores/socket
  default_cpu_threads_per_task = 4
  hardware_threads =

  [[run]]
    use_gpus = true
    use_cpu_mask = false
    use_hostfile = $IO_DISTRIBUTION_OVER_GPUS
    use_mpmd = false
    partition = dolpung
    nodes = 1
    gpus_per_node = $default_gpus_per_node
    stack_size = unlimited
    [[[env]]]
      # OpenMP
      OMP_DYNAMIC = false
      OMP_SCHEDULE = guided
      OMP_STACKSIZE = 500M
      # Unified Communication X
      UCX_HANDLE_ERRORS = bt
      # OpenMPI
      OMPI_MCA_btl = self
      OMPI_MCA_coll = "^hcoll,ml"
      OMPI_MCA_io = romio321
      OMPI_MCA_osc = ucx
      OMPI_MCA_pml = ucx
      # NVIDIA
      CUDA_BUFFER_PAGE_IN_THRESHOLD_MS = 0.001
      NVCOMPILER_ACC_DEFER_UPLOADS = 1
      NVCOMPILER_TERM = trace
      NVCOMPILER_ACC_USE_GRAPH = 1
      # Jemalloc
      MALLOC_CONF = thp:never
    [[[batch]]]
      reservation = dolpung


  [[mon]]
    partition = shared

  [[clean]]
    partition = compute
    tasks = 128
    time_limit = 30

  [[intake]]
    python3 = /usr/bin/env python3

[MPI]
  command = "srun -l --kill-on-bad-exit=1 --export=ALL --gpus-per-node=$$gpus_per_node"
